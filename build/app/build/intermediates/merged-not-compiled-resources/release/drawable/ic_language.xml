<?xml version="1.0" encoding="utf-8"?>
<!-- Globe / language icon — safe on all API levels, matches existing icon style -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/white">

    <!-- Outer circle -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@color/white"
        android:strokeWidth="1.8"
        android:pathData="M12,2 A10,10 0 1,0 12,22 A10,10 0 1,0 12,2 Z"/>

    <!-- Horizontal middle line -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@color/white"
        android:strokeWidth="1.8"
        android:pathData="M2,12 L22,12"/>

    <!-- Left oval (longitude lines) -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@color/white"
        android:strokeWidth="1.8"
        android:pathData="M12,2 C8,6 8,18 12,22"/>

    <!-- Right oval (longitude lines) -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@color/white"
        android:strokeWidth="1.8"
        android:pathData="M12,2 C16,6 16,18 12,22"/>

</vector>